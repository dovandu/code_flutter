Cấu trúc và mô hình app:
assets : 
         - fonts
         - icons
         - jsons: config data
lib: 
     - base:
             + presenter
             + contract: abstract (void updateState();)
             + const
             + utils
      
